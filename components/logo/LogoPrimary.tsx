import * as React from 'react'
import * as Base from 'native-base'

export default function LogoPrimary(): JSX.Element {
  //console.log('*functional* LogoPrimary ')

  return <Base.Circle size={`85px`} bg={`purple.400`} />
}
