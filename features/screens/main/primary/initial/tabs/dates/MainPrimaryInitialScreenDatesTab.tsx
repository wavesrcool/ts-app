import * as React from 'react'
import * as Base from 'native-base'

export default function MainPrimaryInitialScreenDatesTab(): JSX.Element {
  console.log('*functional* MainPrimaryInitialScreenDatesTab ')

  return (
    <Base.Flex safeArea w={`100%`} h={`100%`} align={`center`}>
      MainPrimaryInitialScreenDatesTab
    </Base.Flex>
  )
}
