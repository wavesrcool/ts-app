import * as React from 'react'
import * as Base from 'native-base'

export default function OpenConfirmSmsScreenOrigin(): JSX.Element {
  console.log('*functional* OpenConfirmSmsScreenOrigin ')

  return (
    <Base.Flex
      safeArea
      w={`100%`}
      h={`100%`}
      direction={`column`}
      align={`center`}
    >
      OpenConfirmSmsScreenOrigin
    </Base.Flex>
  )
}
