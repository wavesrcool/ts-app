import * as React from 'react'
import * as Base from 'native-base'

export default function OpenTermsConditionsScreenOrigin(): JSX.Element {
  console.log('*functional* OpenTermsConditionsScreenOrigin ')

  return (
    <Base.Flex
      safeArea
      w={`100%`}
      h={`100%`}
      direction={`column`}
      align={`center`}
    >
      OpenTermsConditionsScreenOrigin
    </Base.Flex>
  )
}
